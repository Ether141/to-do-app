<UserControl x:Class="ToDoAppClient.Controls.ToDoListEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoAppClient.Controls" xmlns:models="clr-namespace:ToDoAppClient.Models" d:DataContext="{d:DesignInstance Type=models:ToDoEntry}"
             mc:Ignorable="d" 
             d:DesignHeight="28" MinHeight="28" MaxHeight="30" d:DesignWidth="500" MinWidth="300" Height="28" Margin="0 0 0 5">

    <Grid Background="{DynamicResource backgroundBrush}">
        <DockPanel>
            <CheckBox Margin="10 0 10 0" VerticalAlignment="Center" IsChecked="{Binding IsDone}"/>
            <Label VerticalAlignment="Center" Content="{Binding Text}"/>
        </DockPanel>
    </Grid>
</UserControl>
